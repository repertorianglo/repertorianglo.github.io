---
import FormattedDate from './FormattedDate.astro';
import { Image } from 'astro:assets';

interface Props {
    href: string;
	heroImage: {
        default: string;
    };
    data: {
        titulo: string;
        subtitulo?: string;
        dataPublicacao: Date;
        grupo?: number;
    };
    grupos: Array<{
        alunos: string[];
    }>;
}
const { href, heroImage, data, grupos } = Astro.props;
---
<a 
    class="post-card p-4"
    href={href}
    style="cursor: pointer; text-decoration: none; color: inherit;"
>
    <div class="postcard-content">
        {heroImage && (
            <div class="hero-image">
                <Image src={heroImage.default} alt={`${data.titulo} - Hero Image`} width={400} height={225} />
            </div>
        )}
        <h2 class="post-title">{data.titulo}</h2>
        {data.subtitulo && <h3 class="post-subtitle">{data.subtitulo}</h3>}
        <div class="post-meta mb-1">
            <div class="post-date"><FormattedDate date={data.dataPublicacao} /></div>
        </div>
        {typeof data.grupo === 'number' && (
            <div class="post-students">
                <strong>Criado por: </strong> {grupos[data.grupo].alunos.join(', ')}
            </div>
        )}
    </div>
</a>

<style>
.post-card {
    background: rgba(255, 255, 255, 0.9);
    border-radius: 12px;
    /* padding: 1.5rem; */
    box-shadow: var(--box-shadow);
    border: 1px solid rgba(var(--primary), 0.1);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.post-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(var(--gray), 35%), 0 12px 32px rgba(var(--gray), 40%);
}

.postcard-content {
    display: flex;
    flex-direction: column;
}

.hero-image {
    width: 100%;
    margin-bottom: 0.5rem;
}

.hero-image img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: 8px;
    object-fit: cover;
}

.post-title {
    margin: 0 0 0 8px;
    font-size: var(--text-lg);
    font-weight: 700;
    color: rgb(var(--primary));
    line-height: 1.3;
}

.post-subtitle {
    margin: 0 0 0 9px;
    font-size: 0.525rem;
    font-weight: 400;
    color: rgb(var(--primary));
    line-height: 1.3;
}

.post-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    font-size: 0.725rem;
    color: rgb(var(--gray));
    margin-left: 9px;
}

.post-date {
    font-weight: 600;
}

.post-author {
    font-style: italic;
}

.post-group {
    background: rgba(var(--blue-anglo), 0.1);
    color: rgb(var(--blue-anglo));
    padding: 0.25rem 0.5rem;
    border-radius: 6px;
    font-size: 0.8rem;
    font-weight: 600;
}

.post-students {
    font-size: 0.725rem;
    color: rgb(var(--gray-dark));
    padding: 0.5rem 0.75rem;
    background: rgba(var(--gray-light), 0.5);
    border-radius: 8px;
    border-left: 3px solid rgb(var(--blue-anglo));
}

.post-students strong {
    color: rgb(var(--primary));
}

@media (max-width: 768px) {    
    .post-card {
        padding: 1rem;
    }
    
    .post-title {
        font-size: 1.25rem;
    }
    
    .post-meta {
        flex-direction: column;
        gap: 0.5rem;
    }
}
</style>